
%header
  = render partial: "toppage/header"
.moving-display
  = render partial: "toppage/slide-display"
.itemlist-1
  %h2.pickup-head
    ピックアップカテゴリー
  = render partial: "toppage/item-display"
 
%aside
  = render partial: "toppage/aside"
%footer
  = render partial: "toppage/footer"
.sell-btn
  = render partial: "toppage/sell-btn"

-# %h4 商品一覧ページ
-# = link_to "test用の新規投稿", new_item_path
-# - @items.each do |item|
-#   %p= item.name
-#   = link_to "商品詳細", item_path(item.id)
-#   = link_to "（仮）削除", item_path(item.id),method: :delete
